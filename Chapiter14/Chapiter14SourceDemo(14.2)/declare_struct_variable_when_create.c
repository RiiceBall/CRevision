#include <stdio.h>
#include <stdlib.h>

// 定义结构体的时候直接声明结构变量
// 可以省略Hero，即只写struct
// struct Hero
struct
{
	char *name;   // 英雄的名称
	int level;    // 英雄的等级
	char *job;    // 英雄的职业
	char *skill;  // 英雄的技能
}jackie_chan, laola, guofurong;  // 声明了三个变量

int main(void)
{
	return (0);
}
