#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	// 在stdio.h头文件中，NULL被定义为常量
	// 指针的初始值为空，表示指针不指向任何地址
	int *ptr_num = NULL;
	char *ptr_name;
	float *money_ptr;
	double *p_price;

	return (0);
}
